'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.validationError = validationError;
exports.printValidationErrors = printValidationErrors;
function validationError(errKey, errMsg, validationErrs) {
  if (validationErrs) {
    var errors = validationErrs.get(errKey) || [];
    errors.push(errMsg);
    validationErrs.set(errKey, errors);
  } else {
    throw new Error(errKey + ': ' + errMsg);
  }
}

function printValidationErrors(verrors) {
  verrors.forEach(function (errors, context) {
    var contextStr = context == null ? 'Found some errors:' : context + ':';
    console.log(contextStr);
    errors.forEach(function (err) {
      return console.log('  • ' + err);
    });
    console.log('');
  });
}